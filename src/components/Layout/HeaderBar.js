
import classes from "./HeaderBar.module.css";

const HeaderBar = () => {
    return (<div>
        <h1 className={classes.cardbody}>The Generics</h1>
        </div>
      );
};

export default HeaderBar;
